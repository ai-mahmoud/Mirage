server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Client-side routing (react-router-dom) — any unknown path falls back
    # to index.html rather than a raw nginx 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
